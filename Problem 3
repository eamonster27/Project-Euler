/*
 * Largest prime factor
 * Problem 3
 * The prime factors of 13195 are 5, 7, 13, and 29.
 *
 * What is the largest prime factor of the number 600851475143?
 */

	//Create linked list for odd factors.
	//Create linked list for prime factors.
	//How will I reference the largest prime factor? End pointer for prime factor linked list[pass by value].
	
	//Find all factors of 600851475143
	//   Eliminate even factors
	//   Store in linked list for factors.

	//For all odd factors - ODD ENFORCED IN FACTOR FINDER
	//   Decide if target factor is prime or not
	//      PRIME COMPUTATION:
	//         BOOLEAN Prime = true;
	//         Modulo target factor by every integer > 2 && <= (target factor/2) truncated
	//            if result = 0[NONPRIME]
	//               Prime = false;
	//               integer = target factor/2 + 1 [BREAK]
	//            
	//               
	//         
	//         If Prime = true: Store target factor in Prime linked list
	//         
	//   Increment target number by 2 and repeat


#include <iostream>
#include <math.h>

using namespace std;

int main() {
	//Variables
	const int one = 1;
	int itself;
	//Computations
	
	//Output

	//Program end
	return 0;
}
